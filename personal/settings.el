;;; settings.el --- Personal settings -*- lexical-binding: t; -*-

(load-theme 'afternoon t)

(set-face-attribute 'default nil
                    :family "Fira Code"
                    :height 140
                    :weight 'normal)

(setq display-line-numbers-type 'relative)

(setq whitespace-line-column 100)
